<Window x:Class="UpperMachine.ProbeControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="探针控制"
        Width="1180"
        Height="760"
        MinWidth="1040"
        MinHeight="680"
        WindowStartupLocation="CenterOwner"
        Background="#F3F5F8">
    <Window.Resources>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40" />
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Margin" Value="0,0,8,8" />
            <Setter Property="Padding" Value="14,0" />
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#1F2937" />
            <Setter Property="BorderBrush" Value="#CBD5E1" />
        </Style>
        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#1D4ED8" />
        </Style>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D8DEE8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="Margin" Value="0,0,0,14" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
        <Style x:Key="LogBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Background" Value="#0F172A" />
            <Setter Property="Foreground" Value="#D6E4FF" />
            <Setter Property="BorderBrush" Value="#1E293B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14">
            <DockPanel LastChildFill="False">
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="控制与日志" DockPanel.Dock="Left" Margin="0,0,14,0" />
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource ActionButtonStyle}"
                        Click="SaveSettingsButton_Click"
                        Content="保存指令" />
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="CloseWindowButton_Click"
                        Content="关闭" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="探针点动" />
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <Label Content="点动步长" />
                                    <TextBox x:Name="StepTextBox" Text="1" />
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <Label Content="点动速度" />
                                    <TextBox x:Name="FeedTextBox" Text="500" />
                                </StackPanel>
                            </Grid>
                            <UniformGrid Columns="2" Rows="2" Margin="0,0,0,8">
                                <Button Style="{StaticResource ActionButtonStyle}" Click="MoveLeftButton_Click" Content="X-" />
                                <Button Style="{StaticResource ActionButtonStyle}" Click="MoveRightButton_Click" Content="X+" />
                                <Button Style="{StaticResource ActionButtonStyle}" Click="MoveDownButton_Click" Content="Y-" />
                                <Button Style="{StaticResource ActionButtonStyle}" Click="MoveUpButton_Click" Content="Y+" />
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="抬笔落笔" />
                            <Label Content="抬笔命令" />
                            <TextBox x:Name="RaiseCommandTextBox" />
                            <Label Content="落笔命令" />
                            <TextBox x:Name="DropCommandTextBox" />
                            <Label Content="保持命令" />
                            <TextBox x:Name="HoldCommandTextBox" />
                            <WrapPanel Margin="0,8,0,0">
                                <Button Style="{StaticResource AccentButtonStyle}" Click="VerifyDropButton_Click" Content="验证落笔" />
                                <Button Style="{StaticResource AccentButtonStyle}" Click="VerifyRaiseButton_Click" Content="验证抬笔" />
                                <Button Style="{StaticResource ActionButtonStyle}" Click="SendHoldButton_Click" Content="发送保持" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="串口日志" />
                    <TextBox x:Name="LogTextBox" Style="{StaticResource LogBoxStyle}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Margin="2,10,2,0"
                   Foreground="#6B7280"
                   Text="所有控制命令都会通过当前已连接的控制串口发送。" />
    </Grid>
</Window>
